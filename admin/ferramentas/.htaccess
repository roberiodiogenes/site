Options -Indexes

# Ferramentas administrativas — libera PHP neste subdiretório
# (o .htaccess pai bloqueia arquivos não listados; este sobrescreve para cá)
<FilesMatch "\.php$">
    Require all granted
</FilesMatch>
